import React from "react";

export const metadata = {
  title: "stocks-dashboard",
  description: "mossAdmin dashboard",
};
const Layout = ({ children }) => {
  return <div>{children}</div>;
};

export default Layout;
